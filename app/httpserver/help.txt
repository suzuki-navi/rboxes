httpserver - Simple HTTP server for serving directories

Usage:
  httpserver [OPTIONS]

Options:
  --port, -p PORT   Port to listen on (default: 8080)
  --help            Show this help message

Examples:
  httpserver                    # Serve current directory on port 8080
  httpserver --port 3000        # Serve on port 3000
  httpserver -p 9000            # Serve on port 9000

Features:
  - Serves static files over HTTP
  - Automatic directory listing
  - Content-type detection
  - Runs in isolated Docker container
  - Preserves file permissions

Notes:
  - Press Ctrl+C to stop the server
  - Access the server at http://localhost:PORT
  - The server runs with your user permissions
