# Developer

When the user specifies target directory as relative path like `./mydir` or `../mydir`, the result format shows by relative path, too.


# AI

Done! I've fixed the issue where trailing slashes in the target directory path would cause duplicate slashes in the output.

## Implementation

Added a regex substitution in the `format_directory_output` function to remove any trailing slash before appending one:

```perl
# Remove trailing slash if present to avoid duplication
$display_dir =~ s/\/$//;
print "$display_dir/\n";
```

## Behavior

Now when you specify a directory with a trailing slash:

```bash
# Input: ./mydir/
# Output: ./mydir/ (not ./mydir//)

# Input: ../otherdir/
# Output: ../otherdir/ (not ../otherdir//)
```

The tool now correctly handles directories specified with or without trailing slashes, ensuring clean and consistent output formatting.

# Developer

実装が完了しました。以下の変更を行いました：

## 実装内容

### 1. オリジナルパス形式の保存
- `$original_target_dir`という新しいグローバル変数を追加
- ユーザーが指定したパス形式（相対パスまたは絶対パス）を保存

### 2. パス変換関数の追加
- `convert_to_display_path()`関数を実装
- 内部で使用する絶対パスを、出力時にオリジナルのパス形式に変換

### 3. 出力の更新
- ディレクトリヘッダー表示時に`convert_to_display_path()`を使用
- エントリパス表示時にも同様に変換を適用

## 動作確認

相対パスで指定した場合、出力も相対パスで表示されることを確認しました：

```bash
# 入力: ./app/summarize-directory-size
# 出力: ./app/summarize-directory-size/... という形式

# 入力: ../app/claude
# 出力: ../app/claude/... という形式
```

これにより、ユーザーが指定したパス形式が出力にも反映され、より直感的で分かりやすい結果が得られます。


# Developer

