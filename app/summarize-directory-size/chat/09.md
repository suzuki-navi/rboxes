# Developer

Please write summarize-directory-size` in `app/README.md`.


# AI

I've added documentation for `summarize-directory-size` to `app/README.md`. The tool has been listed under the "System & File Management" section with this description:

> **summarize-directory-size** - Recursively analyzes and summarizes directory sizes with breakdown of large subdirectories

The tool provides directory size analysis with the following features:
- Recursive breakdown of large subdirectories based on configurable threshold percentage
- Respects `.gitignore` by default (can be overridden with `--include-gitignored`)
- Configurable display limits for entries and file extensions
- Shows size percentages and file extension statistics

The change has been saved to app/README.md:25-27.


# Developer

