# Developer

Please sort $data->{entries} by size descending in `identify_large_dirs`.


# AI

Done! I've updated the `identify_large_dirs` function to sort entries by size in descending order.

The change was made in `summarize-directory-size:328`:

```perl
foreach my $entry (sort { $data->{entries}{$b}{size} <=> $data->{entries}{$a}{size} } keys %{$data->{entries}}) {
```

This sorts the hash keys using a numeric comparison (`<=>`) on the size values, with `$b` and `$a` reversed to get descending order (largest first). Now when processing subdirectories, the larger ones will be examined first.

# Developer
