# Developer

Please explain the architecture of app/office2pdf


# AI

The `src.md` file contains two main code blocks that define the application. Let me walk through each section:

## Dockerfile (lines 1-13)

```dockerfile
FROM debian:bookworm
RUN apt update
RUN apt install -y libreoffice
RUN apt install -y fonts-noto-cjk fonts-noto-cjk-extra locales
RUN apt install -y libipc-run3-perl
RUN locale-gen ja_JP.UTF-8
COPY . /app
```

**Purpose**: Sets up the container environment for Office-to-PDF conversion.

**Key installations**:
- `libreoffice` - Core conversion engine that handles MS Office formats
- `fonts-noto-cjk fonts-noto-cjk-extra` - Google's Noto fonts for Chinese/Japanese/Korean characters, ensuring documents with Asian text render correctly
- `locales` - Locale support package
- `libipc-run3-perl` - Perl module for running external processes with timeout control
- `locale-gen ja_JP.UTF-8` - Generates Japanese UTF-8 locale (important for Japanese document handling)

## main.sh (lines 15-103)

### Initialization (lines 16-26)
```bash
set -e  # Exit on error
script_dir="$(cd $(dirname "${BASH_SOURCE[0]}") && pwd)"
pwd=$(pwd)
volumes=()
```
Sets up error handling and determines the script's location.

### Argument Parsing (lines 33-70)
The script accepts:
- `--help`: Shows usage information
- `-o/--output <path>`: Specifies output PDF path (defaults to `<input>.pdf`)
- `-f/--force`: Overwrites existing output file
- `<PATH>`: Input Office file (positional argument)

**Key validation**:
- Converts paths to absolute using `realpath` (line 45, 65)
- Ensures only one input file is specified (lines 60-64)
- Checks output file doesn't exist unless `-f` used (lines 85-91)

### Temporary Directory Setup (lines 72-77)
```bash
temp_dir=$(mktemp -d)
trap 'rm -rf "$temp_dir"' EXIT
volumes+=("-v" "$temp_dir:$temp_dir")
```
- Creates temp directory for intermediate files
- Sets up cleanup on exit using `trap`
- Mounts temp directory into container

### File Processing (lines 79-102)
```bash
ext="${source_path##*.}"  # Extract file extension
cp "$source_path" "$temp_dir/target.$ext"  # Copy to temp with standard name
$script_dir/rdockrun "${volumes[@]}" $script_dir perl /app/office2pdf.pl "$temp_dir/target.$ext"
cp "$temp_dir/target.pdf" "$output_path"  # Copy result out
```

**Flow**:
1. Extracts file extension from input path
2. Copies input to temp directory as `target.<ext>`
3. Runs `office2pdf.pl` in Docker via `rdockrun`
4. Verifies PDF was created
5. Copies resulting PDF to specified output location

**Why copy to temp?**
- Normalizes filename to `target.<ext>` regardless of input name
- Ensures clean working directory inside container
- Prevents permission issues with mounted volumes

The code demonstrates defensive programming with proper error checking, cleanup handling, and path validation throughout.

# Developer

When no parameters are provided, the following errors occurred:
```
cp: cannot stat '': No such file or directory
```

# AI

You've identified a bug in the error handling. The issue occurs because the script doesn't validate that a source file was provided before attempting to use it.

## Root Cause

Looking at `main.sh:93`:
```bash
cp "$source_path" "$temp_dir/target.$ext"
```

When no arguments are provided:
- `source_path` remains empty (initialized as `""` on line 29)
- The script proceeds to line 93 and tries to copy an empty string
- This causes the `cp: cannot stat '': No such file or directory` error

## Missing Validation

The script should check if `source_path` is empty after argument parsing. Between lines 70 and 72, validation should be added:

```bash
# After the while loop ends (after line 70)
if [[ -z "$source_path" ]]; then
    echo "Error: No input file specified" >&2
    show_help >&2
    exit 1
fi
```

## Why This Wasn't Caught

The existing validation at lines 60-64 only prevents **multiple** input files, but doesn't check for **zero** input files. The logic assumes at least one positional argument will be provided.

## Recommended Fix Location

The fix should go in `src.md` at line 70 (after the `esac` and before `export LC_ALL`), adding:

```bash
if [[ -z "$source_path" ]]; then
    echo "Error: No input file specified" >&2
    show_help >&2
    exit 1
fi
```

This ensures the script fails gracefully with a helpful error message instead of attempting to copy an empty path.

# Developer

Please fix the code as proposed.


# AI

I've applied the fix to `app/office2pdf/src.md`. The validation has been added at lines 72-76, right after the argument parsing loop and before the environment setup.

**Changes made:**
```bash
if [[ -z "$source_path" ]]; then
    echo "Error: No input file specified" >&2
    show_help >&2
    exit 1
fi
```

**What this does:**
- Checks if `source_path` is empty after all arguments have been parsed
- If empty, prints a clear error message to stderr
- Shows the help text to guide the user
- Exits with status code 1 to indicate an error

Now when the command is run without parameters, it will display:
```
Error: No input file specified
```
followed by the help text, instead of the cryptic `cp: cannot stat ''` error.

**Next step:** Run `./build.sh` in the `app/office2pdf` directory to rebuild the executable with the fix.

# Developer

